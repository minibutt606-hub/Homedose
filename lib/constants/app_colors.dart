import 'package:flutter/material.dart';

class AppColors {
  /// Dark teal/blue — used for buttons, progress bar, bold accents
  static const Color primaryBlue = Color(0xFF649DAD);

  /// Light teal/green — used for logo tint, text highlights, links
  static const Color primaryTeal = Color(0xFF66C6BA);

  static const Color textBlack = Color(0xFF1E1E1E);
  static const Color textGrey = Color(0xFF8C8C8C);
  static const Color fillColor = Color(0xFFF5F5F5);
  static const Color background = Colors.white;
}
